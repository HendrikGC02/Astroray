# pkg55 Session C7 — day-arc evidence trail (2026-07-25)

Supervised day session, RTX 5070 Ti workstation, branch
`pkg55-c7-wavefront-finale` (worktree from main @ `e0185c8`). Owner rulings
in-line below. GPU work ran under `.astroray_plan/.orchestrator.gpu.lock`
(one exception noted in §2).

## 1. Phase 0 — the 2× gate + the 0.90× perf reading

**0.90× convicted as a single-sample harness artifact (pkg153 suspect 2B).**
Median-of-5 isolated (cool GPU 48 C, lock held): 1.528× @512spp (spread
1.522–1.545); pytest gate isolated 1.52× PASS + 1.5× target XPASS; full
`wavefront_diff` sweep in-process: XPASS. @1024spp (C7 condition):
1.539× (MK 1.084 s / WF 0.705 s, spreads ≤0.007 s). The overnight 0.90×
(WF 0.647 s) was observed once, by the single-sample harness, during the
contended overnight window; the "rerun on unmodified main" evidence file
contains only the 3 ratio gates — the perf gate was never re-run on main.
No bisect warranted: today's ratio is the all-time high of the record
(1.38 → 1.46 → 1.50 → 1.53).

**The "≥2× vs the Phase-A baseline" acceptance is dead as written.**
Re-running the exact Phase-A profile harness (`benchmarks/wavefront_baseline.py`)
against the pinned `baseline.json` (2026-05-17, `1a3c159`, same GPU):
`multiwavelength_megakernel` cornell_diffuse 19.92 → 113.03 ms/launch
(**5.7× slower**), cornell_glass 21.51 → 134.60 ms (**6.3×**), regs/thread
125 → **188** (past the 158-reg occupancy cliff Phase A documented). Both
pipelines accreted the same 2026-05..07 feature cost; measured against the
Phase-A pin the wavefront is <1×, and no wavefront-side work can change
that. Filed as **pkg155** (absolute-slowdown investigation, owner-approved
doc-only today).

**Owner ruling (Q1):** perf-push first, then rescope to the achieved level,
land today. **Owner ruling (Q2):** the three R-drift ratio gates stay red
under the pkg153 quarantine; C7 retargets the two megakernel-named env gates
(plan R10); `test_gpu_wavefront_final_image_mean_ratio` stays red and
pkg153-owned.

## 2. pkg89 dedicated-lights-into-wavefront-NEE port (commit `f06fdfa`)

Wiring only — the device sampling already existed
(`gpu_nee.cuh::gpu_dedicated_sample`, #489/#500; Cycles
kernel/light/{point,spot,distant,area}.h via the CPU mirrors; CLAUDE.md §6
no-new-algorithm). Deferred parking +3 float lanes (dedEmissionRGB) +1 int
lane (isDedicated); dedGeoScale folded into the parked scale; shadow stage
upsamples dedicated emission via the same RGBIlluminant path
gpu_nee_resolve uses; pkg140 delta lights get MIS wt=1 in the deferred
branch; NEE gate `(numLights+numDed)>0` matches the CPU light_seed gate.

Measured (48², 256spp, md3, seed 424242): WF/CPU mean ratio
[0.9965–0.9973] on dedicated-only POINT (delta — previously rendered
BLACK on the wavefront), dedicated-only AREA, and mixed emissive+point;
WF/MK ~1.000/0.99. New durable gates:
`tests/wavefront_diff/test_pkg89_wavefront_dedicated_nee.py` (3/3 PASS,
vs the CPU oracle so they survive the megakernel deletion).
*Process note:* the first probe run executed while `acquire_lock` had
returned False (pkg152 probe held the lock) — a discipline slip; the run
was parity-only (not timing) and was not re-used for any perf number.

## 3. Perf-push (commit `b40dfba` + part of `f06fdfa`)

- #484 always-on MIS instrumentation stores gated on `captureMis`
  (production shade skips 3 global writes per NEE shade; the PostNEE_MIS
  snapshot harness still captures — gate re-verified green).
- Per-pass counter zeroing fused into `stageRegenKernel` thread 0
  (replaces 3 `cudaMemsetAsync` launches per pass; ~3.6k launches per
  512-spp render; same same-stream ordering).
- Any-hit shadow traversal and the null-TLAS fast path — the other two
  pkg153-doc candidates — were verified ALREADY PRESENT (Phase-B' shadow
  stage; C4 null-TLAS fallback).

Stage profile (contact sheet 512spp, before fusion): shade 141.5 ms (45%,
182 regs), regen 76.0 ms (24%), intersect 65.9 ms (21%), shadow 29.0 ms
(9%). The shade register pressure is pkg155 territory.

**Achieved ratio (median-of-5, lock held, cool 43 C): 1.494× @512spp,
1.500× @1024spp; session spread across clock states 1.476–1.539×.**
Rescoped C7 acceptance floor ≥1.40×: **MET**. Full protocol + absolute
times pinned in `benchmarks/wavefront/megakernel_final_2026-07-25.json`
(the final megakernel record — after deletion it cannot be re-measured).

## 4. Finale (commit `25fdce6`)

Repoint: every GPU integrator name → `cuda_wavefront_render`
(`path_tracer` NEE=on, `multiwavelength_path_tracer` NEE=off, legacy names
same spectral transport); restir-di driver unchanged; pkg64 SMS probe
env-hook moved into the dispatch (`CUDARenderer::runSmsProbe`); JIT
pre-warm warms the wavefront.

Deletion: `src/gpu/path_trace_kernel.cu` + `src/gpu/multiwavelength_kernel.cu`
removed, along with CUDARenderer::render/renderMultiwavelength, the
megakernel launcher decls, curand RNG states/framebuffer, the cuRAND
host-link block, and the pkg87b GPU-cryptomatte surface (intentional
Phase-C drop; CPU cryptomatte is the supported path).

Hidden couplings found by the sweep and fixed BEFORE deletion:
- `uploadGgxGlassTables` (pkg151) and `uploadProfileTable` (pkg54a) were
  called only in the megakernel render entries — the wavefront read zeroed
  tables whenever no megakernel render preceded it in-process. The
  wavefront driver now owns both uploads.

Test dispositions:
- Perf gate rewritten: wavefront-only absolute ceiling (1.0 s @1024spp
  median-of-3, RTX 5070 Ti pin) + the ratio history recorded; live-ratio
  gates retired with the megakernel.
- `test_wavefront_photon_caustic_parity` (WF-vs-MW SSIM) retired —
  self-comparison after deletion; live caustic gate =
  `test_gpu_caustic_parity.py` (energy-ratio + peak vs CPU) now on the
  wavefront route. This also disposes pkg153 failure class 3.
- Megakernel env gates retargeted per plan R10 (names kept for pkg153
  continuity; thresholds unchanged; still red, pkg153-owned).
- `test_tlas_refit.py`: assertions verified still valid; docstring notes
  the skip_upload device-state semantics ended with the megakernel.

Stale-call-site sweep: every deleted symbol
(`launchPathTraceKernel`, `launchMultiwavelengthKernel`, `launchInitRNG`,
`pathTraceKernel`, `multiwavelengthKernel`, `tracePathGPU`,
`sampleDirectGPU`, `tracePathMW`, `sampleDirectSpectralMW`,
`gpu_sampleBandWavelengths`, `ensureFramebuffer`,
`copyCryptoBuffersToHost`, `d_rngStates`, `renderMultiwavelength`,
`cudaRenderer->render`, `tempCudaRenderer`) grepped across
src/include/module/plugins/tests/benchmarks/blender_addon/scripts — all
remaining hits are historical comments/docstrings; zero live references.
Python bindings sweep: no `render_multiwavelength`/crypto-GPU bindings
existed.

## 5. Verification record

(filled at closeout — see the PR body for the final numbers)
