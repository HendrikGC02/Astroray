# Opus 5 mistake log — session 2026-09-19 night → 2026-09-20

Owner request: track every mistake made by Opus 5 agents this session, the lead included. Each row: who, what, how it was caught, impact, fix. Critic = deepseek-v4.1-flash review of Opus 5 output.

| # | who | mistake | caught by | impact | fix |
|---|---|---|---|---|---|
| 1 | lead | Ran the 2026-09-15 owner `gh` block verbatim, including three tracking comments ("owning spec is pkg274") on #722/#723/#724, which #830 had already closed. Should have pruned stale lines first. | lead, reviewing the run output | 3 redundant comments on closed issues | none needed; prune stale lines before running an old command block |
| 2 | batchT lane | A bash heredoc ate backslashes in its patch script; the script aborted, the generator ran unpatched and rewrote three data files with CRLF. | lane, self-caught (diff check) | none shipped; files reverted, edits redone with Edit | known trap (memory `bash-heredoc-backslash-mangling`) — use Edit or os.path.join |
| 3 | lead | Wrote a Python helper through a bash heredoc with a `'\'` literal; the heredoc mangled it (SyntaxError) — the same trap just logged for batchT. | lead, immediate SyntaxError | none; rewritten with Write | write helper files with Write, never through a heredoc |
| 4 | lead | Started a cold CUDA build in the batchP/batchQ worktrees while telling the lanes to start work there; batchP committed engine changes mid-build, so the .pyd was mixed-state. | lead, on build completion | one wasted build (~28 min lock time) | lanes must not touch src/include/module/CMake while a build of their worktree runs (now in every build message) |
| 5 | batchP lane | Passed the new `light_frame` argument on every light, breaking the 7-argument test mocks in `test_pkg213_*`. | lane, call-site sweep | none shipped; fixed in 627bfe67 | call-site sweep before commit (CLAUDE.md) |
| 6 | batchP lane | First numpy IES reference was 1.8 % off at the 360° seam (missed Cycles' float32 wrap check). | lane, A/B vs Cycles | none shipped | — |
| 7 | batchP lane | Commit d601937b message claims both backends match the checker formula at 97–99 %; GPU is 94.8 %. | lane, self-caught | wrong number in a commit message (PR body correct) | — |
| 8 | batchP lane | Wrote a spec Status value that failed `project_index.py lint`. | lane, lint | none shipped | — |
| 9 | batchQ lane | Staged copy of `grid_medium.cpp` normalised 3 CRLF lines. | lane, diff check | none shipped; redone byte-level | — |
| 10 | batchQ lane | Dropped an `#include` that `raytracer.h` include order depends on. | lane, `g++ -fsyntax-only` | none shipped | — |
| 11 | batchQ lane | Hit the known heredoc backslash trap twice; one commit went out without its README change. | lane, self-caught | one incomplete commit, fixed next commit | — |
| 12 | lead | Launched the batchP critic detached (`( … &)`) from a Bash call; it was killed when the call returned (`no_clean_finish` after 96 s). | lead, evidence JSON | 15 min lost; re-run | always `run_in_background` for delegate calls |
| 13 | batchS lane | First "fails before the fix" check stashed the tests together with the code, so it proved nothing. | lane, self-caught | none; redone stashing only exporter.py | stash only the code under test |
| 14 | batchS lane | Heredoc mangled a newline escape in a scratch driver; launcher failed under the PowerShell execution policy; a bridge snippet returned a list where a dict was required. | lane, self-caught | minutes | — |
| 15 | batchR lane | A scene check assumed rendered colour ∝ texture colour, which the spectral pipeline does not preserve; first contact-sheet camera was orthographic (unsupported, #845); a patch script assumed CRLF on an LF file. | lane, self-caught | none shipped | — |
| 16 | batchP lane | Committed + pushed a docs commit after the lead queued its build (HEAD mismatch would abort the build); reset locally, remote keeps the commit. | lane, self-caught | none (build not yet started) | no commits at all between BUILD REQUEST and the result |
| 17 | batchP lane | Cycles output names with dots (`r0.05`) collapsed into one file; first inside-sphere reference case wrong (1.0228 vs MC); told the lead Blender's default lamp radius is 0.25 (it is 0.1 in the startup scene, 0 for a new light). | lane, self-caught | one wrong number in a report; re-renders | — |
| 18 | batchP lane | Claimed an "IES lookup shared by CPU+GPU" while the GPU wiring did not exist yet (critic #4). | deepseek critic | misleading PR wording | PR text corrected |
| 19 | batchS lane | Moved HEAD off the sha queued for its build (two docs commits); a test comment claimed an emission 5→10 repro it had never run; first cost run recorded 0 ms (stale probe wrappers); CPU evidence runs overlapped the batchQ build. | lane, self-caught | one false comment (corrected), a possibly slowed build | — |
| 20 | lead (prior session, Fable) + #831 Opus lane | #831 GUI verification compared worker ON vs OFF only, so both paths being stale passed (material/light/move edits never re-converted). Not an Opus 5 mistake; recorded because it shipped to the owner's installed addon. | batchS lane repro | stale viewport edits in the installed addon since 2026-09-19 | hotfix #850; verify an edit against the pre-edit frame, not ON vs OFF |
| 21 | batchT lane | An empty test-selection grep made pytest run the whole suite with the GPU hidden (killed at 580 s); a manifest patch slice came out empty (`[[scene]]` also in a header comment) — its own guard aborted it. | lane, self-caught | ~10 min | — |
| 22 | batchS lane | Profiler state kept where a Blender file reload clears it; the orbit/storm/preview timing runs were lost and need another GPU slot. | lane, self-caught | one lost GPU session (~11 min lock) | keep driver state in a module that survives `load_post` |
| 23 | batchS lane + lead review | Hotfix #850 missed a call site: `test_viewport_parity_harness.py::test_pkg81_harness_smoke` still asserted transform_edit → upload_geometry ≥ 1; CI went red. The lane's 19 flipped tests and the lead's review both missed it. | CI | one CI cycle (~40 min) | lead fixed (276e55fa); grep every test that reads dispatcher upload counts before pushing a routing change |
| 24 | batchQ lane | Round-1 CPU blackbody normaliser memoised per kelvin and the doc claim "the GPU normaliser is the CPU's" were wrong (luminance vs Cycles 0.095 at 60.5 K, 7 % jump across 500.5 K). Also overwrote a tracked batchK PNG from a test run (restored). | deepseek critic #4 (confirmed) | would have shipped a CPU≠GPU blackbody mismatch | shared `blackbody_lut.h` on both backends |
| 25 | batchT lane | A queued GPU job launched `bash` from Python, which resolved to WSL's System32 bash (no C:/ paths); it got the lock after 26 min and exited 127 in seconds. | lane, self-caught | one wasted lock slot + 26 min wait | call Git Bash by full path (or PowerShell) from Python |
| 26 | batchR lane | Assumed GPU upload order follows the quad's screen position (wrong; corrected a test comment); first background suite run had no timeout and sat behind batchP's lock (stopped by exact PID); first hand-staged addon failed the build_report manifest check. | lane, self-caught | minutes | — |
| 27 | batchS lane | Staged-addon copy used rmtree on a directory another process held (WinError 5), then overlaid the .pyd without handling the addon's build-ID check — two wasted GPU-lock slots. | lane, self-caught | two lock slots | fresh dir per run; drop build_report.json only in the scratch copy |
