- Line 8 (and 113): “standalone” activation and “pkg51-B” conflict with the 2026-09-22 plan: pkg133 is merged design and its implementation is pkg51 Phase 1. Fix: remove alternate routing and state “dispatch only as pkg51 Phase 1 in Stage 3.”

- Lines 19–24, 38–40, 63, 78: one SRF layer collapses wavelength information, but pkg51 Phase 3 requires Phase 1 to retain spectral bins for chromatic PSF-before-integration and bin-convergence. Fix: require wavelength-resolved raw layers/bins (plus named SRF outputs) and specify their hand-off to Phase 2.

- Lines 20, 38, 46–47: defining SRF as filter × QE loses the separately retained throughput and QE required by the Stage 3 plan and pkg51’s detector equation. Fix: store `T(λ)` and `QE(λ)` independently; use their product only for the Phase-1 proposal/response weighting.

- Lines 23–24 and 121–123: “correct per-channel photon statistics” overclaims Phase 1, which produces relative response-weighted radiance; calibrated photon/electron/ADU statistics are blocked on pkg243 Phase 2. Fix: describe Phase 1 as unbiased relative radiance accumulation with provenance.

- Lines 41–47, 60–62, 73–75: “hero-aware” and `SRF/pdf` do not define the composed sampling measure, support rule, or estimator required to demonstrate unbiasedness; a single within-noise comparison cannot establish it. Fix: specify the combined proposal’s normalized support, its composition with the hero-wavelength PDF, exact likelihood-ratio estimator, and analytic flat/narrow/multi-channel unbiasedness tests.

NO — the merge note is partly right, but stale routing and Phase-1 data/estimator requirements are inconsistent with the plan’s chromatic, separately parameterized downstream pipeline.

VERDICT: REVISE - Align dispatch with pkg51 Phase 1 and require unbiased, wavelength-resolved, separately retained throughput/QE output.
