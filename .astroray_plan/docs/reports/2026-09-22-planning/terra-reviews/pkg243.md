Concrete defects:

- Spec lines 89–93: “prove” semantics in Phase 0 but never defines the exported estimator. Fix: declare the exact quantity, e.g. \(Q_\mathrm{band}=\frac1N\sum_i L(\lambda_i)\) for uniform wavelength samples, its wavelength interval, and that it is a relative, dimensioned-or-dimensionless scalar.
- Spec lines 78–80, 90–93: does not require capture of the pre-XYZ/pre-sRGB scalar into a separate framebuffer/export plane. Fix: specify that the raw channel receives the integrator’s scalar before XYZ/RGB conversion, tone mapping, denoising, colourmaps, gamma, or clamping.
- Spec lines 99–101: Phase 2 only checks round-trip/backend behaviour; it omits Stage 3’s required normalisation bridge. Fix: add scene-length-unit declaration, emissivity-to-radiance equation \(I=\int j\,ds\), explicit observer-pixel solid-angle convention, and physical radiance units/conversion.
- Spec line 126: “No calibrated radiance” conflicts with Stage 3’s required Phase 2 physical-radiance normalisation. Fix: limit the non-goal to detector/exposure/photon-count calibration; require radiance normalisation in Phase 2.
- Spec lines 109–115: acceptance tests do not validate dimensional units, path-length scaling, solid-angle handling, or physical radiance. Fix: add analytic fixtures proving each bridge component and metadata assertions for units and conventions.

NO — the display separation is intended but not specified at the scalar storage boundary, and the mandatory Phase 2 normalisation bridge is absent.

VERDICT: REVISE - Define the raw estimator and pre-display storage boundary, then add the Stage 3 scene-length, solid-angle, and physical-radiance Phase 2 contract.
