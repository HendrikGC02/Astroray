- Lines 95–102: `Q_band` is called a mean of “radiance” but also dimensionless; its per-pixel/spp estimator and code-unit scale are not defined. Fix: define the full pixel estimator (including camera samples and wavelength PDF) and label its raw unit unambiguously as a relative spectral-radiance code unit.

- Lines 124–131, 148–152: “scene-length-unit factor” is not a dimensional conversion from an arbitrary relative scalar to physical radiance. Fix: define a calibrated emissivity/source scale and equation, e.g. \(I_{\lambda,\mathrm{SI}}=u_{\mathrm{m/BU}}\,C_j\,Q_\mathrm{band}\), then \(I_\mathrm{band}=\Delta\lambda_\mathrm{nm}I_{\lambda,\mathrm{SI}}\), with units for every term.

- Lines 144–152, 172–174: the bridge does not require exporting the numerical normalization inputs/formula per output. Fix: require provenance for metres-per-scene-unit, emissivity calibration constant and units, wavelength estimator/PDF, \(\Delta\lambda\), per-pixel \(\Omega_\mathrm{pix}\), conversion formula/version, and whether the exported plane is radiance or pixel irradiance.

NO — the raw plane is correctly isolated from display processing, but the Phase 2 bridge cannot yet convert its stated relative quantity into reproducible physical radiance.

VERDICT: REVISE - Specify the calibrated source-to-SI conversion and complete exported estimator/provenance contract.
