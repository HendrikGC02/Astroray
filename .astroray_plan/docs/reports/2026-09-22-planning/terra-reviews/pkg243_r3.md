- `pkg243:80, 95–103, 113–119` — The final exported pixel estimator omits camera-sample/film-weight accumulation, and its specified capture point is ambiguous against the current pre-existing `max(0, …)` clamp. Fix: define the complete per-pixel weighted Monte Carlo estimator and explicitly store `Q_band` before the clamp, retaining clamping only for display.

- `pkg243:127–140` — `u·C_j·Q_band` treats a generic renderer scalar as a volumetric-emissivity path integral; it is invalid for arbitrary surface, environment, or lamp contributions, and `C_j = 1` cannot justify SI-labelled output. Fix: restrict this conversion to a named, validated emission-only volume contract, or define distinct calibrated source/radiance contracts and mark all other outputs relative.

- `pkg243:157–161` — `w_pix h_pix / f²` is only the on-axis small-pixel approximation, yet the required provenance claims per-pixel solid angle. Fix: specify and test the off-axis perspective-pixel solid-angle formula (or an exact corner-based solid angle), with the current formula restricted to an on-axis fixture.

NO — the raw plane is intended to be display-independent and the bridge is outlined, but its exported estimator and physical normalisation remain unsafe to dispatch as written.

VERDICT: REVISE - define the complete unclamped pixel estimator, constrain or validate the SI conversion domain, and correct per-pixel solid angle.
