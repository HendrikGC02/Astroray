- Spec L104: It names a nonexistent public `astroray.cuda_wavefront_render_restir(...)`; the reachable GPU ReSTIR route is `Renderer.render()`, which passes no band/mode and hardcodes 380–780/visible output. Fix: replace the phantom entrypoint with that real route and require resolved v1 controls to be passed or explicitly rejected before dispatch.

- Spec L131–140: The direct GPU binding’s boolean cannot express or validate the four-mode enum, nor can it conflict with an explicit string mode it does not accept. Fix: give the binding an `output_mode` string contract (with a defined legacy-bool migration) and require exact enum validation/output mapping.

- Spec L151–159: “Every output carries” provenance, but no carrier is specified for the bare NumPy array, CLI output, or Blender result. Fix: define a per-entrypoint metadata transport (returned record, file sidecar/embedded metadata, Blender result metadata) and test it.

NO — the reachable ReSTIR backend ignores the contract, while the direct GPU API cannot receive the declared explicit mode contract.

VERDICT: REVISE - ReSTIR routing, direct-binding mode representation, and provenance delivery must be specified before callers can uniformly deliver v1.
