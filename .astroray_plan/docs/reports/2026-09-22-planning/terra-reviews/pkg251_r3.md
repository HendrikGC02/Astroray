- Spec lines 100–105 and 138–140 misidentify the Python render routes: `Renderer.render()` also dispatches GPU wavefront/ReSTIR, while `cuda_wavefront_render_restir` is not a direct Python binding. Fix: enumerate `PyRenderer.render()` CPU, wavefront-GPU, and ReSTIR-GPU branches as actual routes, and classify ReSTIR as internal unless a public binding is added.

- Spec lines 151–159 leave `rgb`/`srgb` transforms and pixel layout unspecified, so backends cannot deliver identical explicit mode payloads. Fix: pin matrix, white point, transfer/clamping policy, channel order, and per-mode array shape in v1.

NO — the actual Python GPU/ReSTIR caller path and the mode payload definition remain insufficiently explicit for safe dispatch.

VERDICT: REVISE - Correct the route matrix and fully define each output-mode payload contract.
