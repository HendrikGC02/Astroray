- Lines 95–101, 190–191: The manifest has no enforced GREEN predicate or verified-evidence schema; “a number or evidence path” permits dangling or non-gating artifacts. Fix: require per-row status derived by a schema-validated predicate, artifact hash/existence check, and all mandatory measurements.

- Lines 114–125, 133–136: Frozen exclusions/population are neither hash-locked nor ratified, and PROVISIONAL is not forbidden from GREEN. Fix: record immutable input-manifest SHA/exclusion rationale and make unratified coverage categorically ineligible for GREEN.

- Lines 118–123: A `SUPPORTED` classification requires variant coverage but no per-backend, per-variant proof artifact; unsupported claims can therefore raise the score. Fix: default unproven claims to 0 and require linked rendered/test evidence for every nonzero classification.

- Lines 142–146: Gate (a) omits the authoritative 3×100 repetitions, numeric limits, GPU-only latency condition, and denoise exclusion from its executable predicate. Fix: encode all north-star §2(a) conditions in the instrument output and GREEN evaluator.

- Lines 148–154, 204–206: Gate (c) requires a GPU execution slot but not successful CPU-and-GPU F12 results with paired artifacts; a CPU-only report could be represented as complete. Fix: require separate CPU/GPU render exit status, SHA-pinned images, and metrics before GREEN.

- Lines 163–166: Gate (d)’s mean-error and detail-preservation checks have no thresholds, so arbitrarily weak checks can pass. Fix: predeclare numeric limits and reference-comparison regions in the test/manifest.

- Lines 173–176: “LIVE open-issue population” has no recorded query timestamp, issue-ID snapshot, or reconciliation rule, allowing omitted issues. Fix: store the exact GitHub query result and require every returned issue to have an independent signed rating.

NO — the spec still permits GREEN through unverified support classifications, unratified denominator inputs, and incomplete gate evidence.

VERDICT: REVISE - Make GREEN a schema-enforced result of complete, hash-pinned, backend-specific evidence.
