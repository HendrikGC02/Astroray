- **Lines 102–119:** The schema contract does not require a structured, hash-locked evidence set per required submeasurement; a single `evidence_path` cannot represent gate (b)’s per-variant proof or all gate (c) artifacts. Fix: require typed evidence arrays with SHA-256, producer command, and mandatory artifact keys per row/subcheck.

- **Lines 196–199:** “Exact `gh issue list`” is not an exact, exhaustive query; default pagination can omit open issues. Fix: prescribe repository, `--state open`, pagination/limit, machine-checked total count, and require the final snapshot hash/count in GREEN evaluation.

- **Lines 201–205:** Gate (f) specifies documents and manually collected evidence, but no scripted instrument validates clean profile, ZIP identity, installer path, absent toolchain/source fallback, and F12 result. Fix: add a gate-(f) capture/validator that emits hash-locked structured checks and makes each mandatory check GREEN-required.

Controlling question: **NO** — these gaps still allow GREEN from incomplete or unsupported evidence.

VERDICT: REVISE - Define complete evidence schemas, exhaustive live-issue capture, and a machine-validated clean-install instrument.
