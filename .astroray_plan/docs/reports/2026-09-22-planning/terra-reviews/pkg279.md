- **L96–103, L173:** Geometry remains symbolic; no frozen values, units, observer convention, disk parameters, or control inclination are specified. Fix: declare all numerical scene/control parameters and coordinate conventions in `gates.toml`/notes.

- **L107–120:** The spherical point-emitter analytic control is not required as a separate Blender-rendered test from the Roche-plus-slim-disk deliverable. Fix: require a dedicated sphere/point Blender control scene and compare its measured contacts directly to the analytic solution.

- **L119–120, L150–151:** Eclipse depth and ingress/egress “match” without numerical tolerances or a defined extraction method. Fix: set frozen absolute phase and depth tolerances, normalization baseline, and contact-detection algorithm.

- **L126–133:** Four contacts are defined using one projected radius, but the cited slim disk is a finite-thickness volumetric emitter with inner/outer radii; its silhouette and full-occultation condition are undefined. Fix: prescribe its projected opaque geometry/orientation or define contacts through a reproducible flux-based criterion.

- **L121, L136, L149:** No samples, seeds, confidence interval, or uncertainty-allocation rule makes the 2% duration budget distinguishable from Monte Carlo and phase-estimation error. Fix: require repeated seeded renders and a CI/extraction-error budget strictly below the acceptance tolerance.

NO — the intended 2% duration threshold exists, but unspecified geometry, contact measurements, emitter silhouette, and statistical budget prevent an unambiguous independent recovery claim.

VERDICT: REVISE - Freeze a separate analytic Blender control and fully specify geometry, contact metrics, and uncertainty allocation.
