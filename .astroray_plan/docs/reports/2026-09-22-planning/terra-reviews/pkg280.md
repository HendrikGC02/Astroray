- **Line 16–17 / 83–84:** It treats volumetric ADAF/synchrotron transport as an intensity `g³`-factor audit, but the cited research requires invariant medium transport (`jν/ν²`, `αν`, fluid-frame frequency); the listed `.cpp` files are registration stubs, while the active fallback is in `black_hole.h` and implementation headers. **Fix:** name the actual driver/headers and require tests of `J`, `A`, fluid-frame frequency, and no Doppler/redshift double-counting.

- **Line 76 vs. 126–129:** The test-file promise covers disk, ADAF, and synchrotron, but the scope makes only thin-disk validation mandatory and permits volume paths to remain merely “audited.” **Fix:** require analytic transfer validation for each path that can be labelled science-ready; explicitly prohibit science release otherwise.

- **Line 100:** “Converted to per-λ” is underspecified at the exact point a missing Jacobian would reintroduce the error. **Fix:** state `Iλ,obs(λ) = (c/λ²) g³ Bν(c/(gλ),T) = g⁵Bλ(gλ,T)` and test equality bin-by-bin.

- **Line 103:** “Energy conservation” conflicts with the next required result: observed bolometric intensity scales as `g⁴`, not conserved. **Fix:** replace it with “invariant residual plus bolometric `g⁴` scaling,” defining integration measure and tolerance.

- **Line 115–116:** A published-frame fallback lacks required matched geometry, emission model, spectral band, normalization, and measurement procedure, so a 5% mismatch is not interpretable. **Fix:** require those frozen comparison metadata and reject a reference that cannot supply them.

NO — the `g⁵` wavelength and `g³` frequency formulas are mutually consistent, but the plan does not yet establish the necessary volumetric transfer convention for every science-released path.

VERDICT: REVISE - Make invariant emissivity/absorption transport mandatory for any volumetric science path and remove the validation/scope ambiguity.
