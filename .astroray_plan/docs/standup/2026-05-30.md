# Morning standup — 2026-05-30 (overnight: general-caustics foundation)

## TL;DR

The lead-track foundation **pkg109 (world-space photon-map kd-tree) shipped and
merged** — the primary overnight success criterion. A parallel small fix
(**pkg76 Gap 2**) also merged. The next lead package (**pkg110**, BSDF-driven
photon bounce) is **validated on a glass sphere but parked as WIP** — its general
loop is correct for solid glass, but the prism rainbow hue gate doesn't reproduce
yet; I filed a finding rather than thrash on tuning.

## Shipped (merged to main)

1. **pkg109 — photon-map kd-tree** ([PR #395](https://github.com/HendrikGC02/Astroray/pull/395), `bc3464b`).
   - `include/astroray/photon/photon_map.h`: balanced kd-tree + bounded-max-heap
     k-NN + Jensen cone-filtered irradiance density estimate. Cited Jensen 2000
     Course 8 (Fig. 7 / Fig. 10 / Eq. 8 / §3.2.1) + pbrt-v4 disk-area factor.
   - Rewired `light_tracer_caustic` from the 2D grid to the kd-tree (k-NN gather,
     density-adaptive radius + brightness auto-scale). Prism regression preserved.
   - `tests/test_photon_map.py` (4 tests) validates the C++ kd-tree vs a numpy
     float64 brute-force oracle exactly, via new `_photon_map_*` pybind bindings.
   - Discipline: numpy prototype validated first; full local suite **1155 passed**;
     stale-call-site sweep clean; CI green; squash-merged.
2. **pkg76 Classroom Gap 2** ([PR #394](https://github.com/HendrikGC02/Astroray/pull/394), `563ab79`).
   - +8 non-Principled BSDF node types in the .blend shader-graph walk
     (Glossy/Translucent/Transparent/Anisotropic/Add Shader/Velvet/Sheen/Toon) +
     bpy-free tests (12 pass). GPU SSIM gate deferred (no GPU in CI).
   - Implemented by a background `package-implementer` in its own worktree, in
     parallel with the pkg109 lead track.

## Mid-flight (WIP, branch pushed, NOT merged)

- **pkg110 — BSDF-driven photon bounce** (branch `pkg110-bsdf-photon-bounce`).
  - The hard-coded 2-face prism refraction is replaced by a **general BVH +
    `Material::sampleSpectral` photon loop**: any glass shape, TIR, multi-bounce,
    hero-λ dispersion + a general (non-triangle) caster-bounds scan.
  - **Validated on a new glass-sphere scene**: a focused "burning-glass" caustic,
    peak luminance 0.79 vs floor median 0.016 (~48× concentration) — proving the
    general loop refracts enter→exit correctly through a solid.
  - **Blocker**: the prism `hue_spread` gate doesn't reproduce under the general
    loop (0.52–0.60 vs 0.70; bright_coverage 0.74–0.77 passes). The stochastic-
    Fresnel + solid-prism deposit yields a different floor spectrum than the
    explicit deterministic 2-face deposit; hue_spread is non-monotonic in photon
    count. **Filed a finding and stopped** rather than thrash on tuning.
  - Resume plan (in `.astroray_plan/docs/pkg110-status-finding.md`): strip the
    radiance η² for power transport; Fresnel-weighted (non-stochastic) deposit to
    match spectrum density; restrict emission to slant faces; re-confirm hue (or
    re-derive the prism ROI/threshold for the solid prism with owner sign-off);
    finalize the glass-sphere concentration gate. Then pkg111.

## Blocked / not attempted (by design)

- **GPU-gated packages** (pkg64-gpu multi-IOR, pkg55-B' CUDA sessions, pkg86-B GPU
  light tree): CI has no GPU — correctness gates can't be verified. Untouched.
- **pkg100 / pkg101 / pkg102**: already on main (stale origin branches). No work.

## Repo state

- `main` clean: pkg76 + pkg109 merged; local `.pyd` rebuilt against main.
- Cosmetic: the `Astroray-pkg76gap2` worktree dir is OneDrive-locked (couldn't
  `git worktree remove`); harmless — prune at convenience. Remote pkg76 branch
  deleted.

## Process notes

- A 3-agent research workflow (Jensen 1996 / pbrt-v4 / Astroray reuse audit) ran in
  the background and front-loaded §6 citations + integration file:lines for the
  whole pkg109→111 chain — paid off across the night.
- The "re-verify vs current main" instruction correctly caught pkg100/101/102 as
  already-done, avoiding wasted re-implementation.
