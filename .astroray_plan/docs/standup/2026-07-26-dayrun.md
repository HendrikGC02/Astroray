# Day standup — 2026-07-26 (continuing from the overnight run)

Follows `.astroray_plan/docs/standup/2026-07-25-overnight.md`. The owner reviewed
the overnight findings, chose **"fix the CPU LUT"** on the pkg160 fork, and asked
me to resolve the remaining action items myself.

<!-- STATUS: running -->

## Owner decision on pkg160 — and why the fix got better

The owner picked **fix the CPU**, not the GPU. Investigating the term to
implement that, I found it has **three independent defects**, not the one Step 0
identified:

1. **Wrong table** — the Step-0 finding: `GGXEnergyCompensationLUT` estimates
   `E` with 256 uniform-hemisphere samples, cannot resolve a narrow GGX lobe, so
   `E → 0` as roughness → 0 and `Fms` pins near its `1/π` ceiling.
2. **Missing cosine.** `AGENTS.md:87` states `Material::eval()` returns
   **brdf × NdotL**. `singleScatter = F*D*G / (4*NdotV + 0.001f)` correctly bakes
   the cosine in (the `NdotL` cancels against the Cook-Torrance denominator). But
   `multiScatter = albedo_ * (Fms * msWeight * 1.3f)` carries **no `NdotL` at
   all** — it violates the eval() contract outright.
3. **An invented weight.** `msWeight = roughness*(2-roughness)` and the `* 1.3f`
   appear in no published formulation. That is a hand-tuned fudge factor, which
   CLAUDE.md §6 forbids.

**The fix is therefore not "repair the LUT" but "delete its role".** `metal.cpp`
is the *only* consumer of `GGXEnergyCompensationLUT` / `ggxMultiScatterCompensation`
(verified by grep), and the repo **already contains the correct implementation**
in the Disney path — `disney.cpp:96 ggxCompensationFactor`, which reads the
shipped Cycles-derived tables, is applied **multiplicatively** to the
single-scatter result at `disney.cpp:652`, and is cited to Kulla & Conty 2017 /
Cycles `microfacet_ggx_preserve_energy` (BSD-3-Clause). Its exact GPU twin
`gpu_ggxCompensationFactor` already exists and is already used at
`gpu_materials.h:904`.

Applying that same pattern to plain `metal` fixes all three defects at once — the
correct tables, the cosine inherited free because the multiplicand already
carries it, and no invented weight — and makes CPU/GPU parity **structural**
rather than something to keep chasing. It is also pure borrowing, no derivation.

Dispatched as a pkg160 rework on PR #527. Note there is a **third** copy of the
broken helper at `src/gpu/wavefront/stage_shade_metal.cu:114` that must be
reconciled, not left as a variant.

**Expect renders to change:** every rough metal gets *dimmer on the CPU*. That is
the intended consequence of the decision. Gates keyed to CPU metal brightness may
move; they are to be reported, not silently re-blessed.

## Action items resolved

| # | Item | Resolution |
|---|---|---|
| 4 | CUDA v12.6/v12.8 split | **Fixed** (`609d70f`). `build_cuda_worktree.bat` now inherits `CUDA_PATH` instead of hardcoding v12.6, and fails loudly if nvcc is missing. Safe now precisely because pkg155 Phase 1 was measured entirely on 12.8 — aligning *removes* a Phase-2 variable rather than introducing one. |
| 3 | pkg157's two unsatisfiable contract clauses | **Amended** (`609d70f`), each with the measurement behind it. Item 2's "BYTE-IDENTICAL" → the 1e-5 MC convention; item 3's firefly clause → deferred to pkg161. Status flipped to done. |
| 6 | `launchStageInit` phantom overload | **Fixed and MERGED** — PR #528, `05b6b49`. The phantom-overload class is now closed: four found, four resolved. |
| 5 | Blender motion blur broken since pkg88-A | **Fixed and merged** overnight in #525. |
| 7 | Orphaned worktree directories | **Still owner's** — OneDrive holds file locks and recursive force-delete is blocked in this session. Disk hygiene only. |

## PR #528 — the last phantom overload, and a sweep that corrected itself

`gpu_wavefront_state.h` declared `launchStageInit` with 6 parameters while the
definition takes 8. pkg157 removed three duplicates of this pattern but correctly
left this one, because fixing it needs the `= 0` default removed or relocated
(a defaulted parameter cannot precede non-defaulted ones) — behaviour-affecting,
and unverifiable without a build the implementer could not run.

Resolution: the default is simply removed. All 6 call sites pass `sample_index`
explicitly, so it was never used.

**Verified rather than assumed:** full Release+CUDA build succeeds, and the change
is measurably inert — same scene/seed against the pre-change binary gives
**identical image sums** (22315.917969 both), max difference 2.48e-07 relative to
peak, i.e. the wavefront's own atomic-ordering noise floor and ~40× inside the
1e-5 convention.

**A methodological note worth keeping.** I swept every `void launchStage*`
declaration against its definition. The first pass flagged **three** mismatches —
including one in pkg157's *already-merged* code. That was my own parser counting
commas **inside parameter comments**. After stripping comments, pkg157's fix is
clean and exactly one real mismatch remains:
`launchStageInit(IntegratorStateSoA&, …)` at `integrator_state_soa.h:124`, a
4-param Phase-A.1 overload with **no definition and no caller**. Dead scaffolding;
reported rather than deleted per CLAUDE.md §3. The lesson: a textual sweep over
C++ signatures is worthless unless it strips comments first, and I nearly filed a
false regression against merged code because of it.

## Post-merge health check — clean

pkg157 and pkg88-B merged within minutes of each other, and nothing else verifies
the two landing *together*. Rebuilt `main` (fresh `.pyd` 11:36 against HEAD 11:33)
and ran the full suite on the RTX under the GPU lock:

**`4 failed, 1531 passed, 69 skipped` in 386.94s** — exactly the overnight
declared baseline's four known exceptions (`test_blender_parity_matrix_generation`
OneDrive `rmtree` flake, plus the three pkg153-quarantined env-scene ratio gates),
and **+25 passing tests** from the two merges. **No regressions from either.**

Two methodology notes, both of which cost me a false alarm and are worth keeping:

1. **Run the suite from the repo root, not from `tests/`.** My first pass reported
   6 failures including
   `test_kerr_validation.py::test_reference_fixture_source_documents_license_fence`
   — which reads *a license fence*, so it looked serious. It is a **CWD-dependent
   test**: it opens `scripts/generate_gyoto_references.py` by relative path, which
   only resolves from the repo root. Nothing was wrong with it. Worth fixing the
   test to resolve paths relative to `__file__`, but it is not a regression.
2. **Some xfails are flaky and therefore meaningless.** Across three runs the
   tallies moved between `25 xfailed / 2 xpassed` and `24 xfailed / 3 xpassed`.
   The two XPASSes are pre-existing deferred markers
   (`test_pkg64_gpu_phase3_prism_psnr_floor`, frozen as legacy by owner decision;
   `test_disable_reflective_caustics_reduces_mirror_caustic_outliers`, deferred).
   A **non-strict xfail that flips between runs asserts nothing** — and it is a
   trap, because un-xfailing it on a lucky run produces a permanently intermittent
   test. Given memory `xfail-gated-features-must-unxfail` already flags xfails as a
   problem area here, these two want either `strict=True` or retirement. Filed as
   an observation, not actioned — neither is in today's scope.

## pkg155 — a hypothesis I formed, tested, and killed

The GPU is compute capability **12.0**; the build targets `75;86;89`, justified by
a `CMakeLists.txt:54` comment citing CUDA 12.6 — while **12.8 is installed**.
`cuobjdump --list-elf -all` confirms the module ships SASS for sm_75/86/89 plus
matching PTX and **nothing for sm_120**, so every kernel on this machine runs via
**driver JIT from compute_89 PTX**. All of that is true and verified.

The inference — that JIT was causing the 221-register blow-up behind the ~5× —
is **false**. Controlled test, identical source at `60306a9`, only
`CMAKE_CUDA_ARCHITECTURES` differing:

| scene | sm_89 (JIT) | +sm_120 (native AOT) | |
|---|---|---|---|
| cornell_diffuse | 97.84 ms/render | **175.62** | **1.80× SLOWER** |
| cornell_glass | 123.03 | **206.10** | 1.68× slower |
| `shade_bucketed` | 239.33 ms, 221 regs | **603.89 ms, 229 regs** | 2.52× slower |

CUDA 12.8's offline `ptxas` produces materially worse Blackwell code than the
driver's JIT. **`75;86;89` is not merely acceptable, it is optimal here.**

What it settles: **the 221-register problem is intrinsic to the kernel, not a
build artifact** (native AOT gives 229), so Phase 1's recovery direction stands
unchanged — per-material shade split, `__launch_bounds__`, live-state audit. And
a plausible-looking lever is now ruled out *with numbers*, before a package was
spent on it. Full write-up: `.astroray_plan/docs/pkg155-sm120-negative-result.md`.

**Two methodological corrections, both mine:**
1. **`-Xptxas -v` counts are worthless under `-rdc=true`** — they read 127 (sm_89)
   and 40 (sm_120) against real post-link values of 221 and 229. I flagged that
   caveat when I took the numbers and then let them drive a hypothesis anyway; a
   caveat is worth nothing until it changes behaviour. **This kills the
   "compile-only bisect"**: pkg155 Phase 2 needs the GPU at every point and cannot
   parallelise with HW verification. Protocol corrected by the architect
   (`6527d60`), which re-scoped the bisect to an opportunistic gap-filler
   (~1–1.5 GPU-hours across 2–3 nights) with HW gates for active PRs always
   outranking it.
2. **Do not conclude from a tool invocation the tool flagged as incomplete** — my
   first `cuobjdump --list-ptx` reported "no PTX at all"; its own output said to
   retry with `-all`.

Tested in a throwaway detached worktree so the CMakeLists edit never touched the
repo — build-file changes are owner-gated, and in the end there was nothing to
propose.

**Worth reinforcing rather than burying:** both protocol-level mistakes today —
the architect's compile-only premise and my JIT hypothesis — died the same way,
by a controlled measurement whose author then published it against themselves.

## pkg160 — plain `metal` was CREATING energy, and the gate could never have caught it

The owner's "fix the CPU" decision turned out to uncover a conservation-of-energy
violation. White furnace, linear, green channel:

| roughness | before | after |
|---|---|---|
| 0.15 | **1.6434** | 0.8823 |
| 0.60 | **1.4069** | 0.8092 |
| 0.90 | **1.7690** | 0.8802 |

**Why no test caught it — this part is systemic, not a pkg160 detail.**
`base_helpers.render_image()` defaults to `apply_gamma=True`, and the gamma path
**clamps to [0,1]**. I verified it independently on the pre-fix binary: a white
furnace reads **linear max 4.139 with 18,338 of 27,648 pixels above 1.0**, and
**gamma max exactly 1.000000 with zero pixels above 1**. So
`test_metal_furnace_energy_above_threshold_all_roughness` recorded a comfortable
**0.9978** against `> 0.78` for a configuration reflecting 4× the incident light.
A gamma-rendered furnace test *cannot* fail on energy gain — and a `> threshold`
assertion has no upper bound anyway. Now in memory as
`gamma-furnace-cannot-detect-energy-gain`.

Corroboration that the fix is right, not just dimmer: glossy-vs-principled-metallic
MSE **0.02474 → 0.00353**, 7× closer.

Also found: **`stage_shade_metal.cu` is dead** — `launchStageShadeMetalGPU` has a
declaration and a definition and *no call site*. Its copy of the helper was a
placeholder `return 0.0f;` whose comment claimed it mirrored `metal.cpp`, making
that whole term a silent no-op. Fixed rather than deleted, since pkg55/128/129
cite the file as a template; deletion flagged as an owner call.

### HW gate: 31 passed, 1 failed — and the failure is pre-existing architecture

`test_plain_metal_gpu_cpu_parity[0.9]` channel B: **1.0722** vs `[0.95, 1.05]`.
Not a pkg160 defect. CPU `evalSpectral` computes compensation **per wavelength**;
the GPU computes it **per RGB channel** and upsamples. Those agree only for a flat
spectrum. Three isolations pin it:

1. **`r=0.05` is at parity** (0.9977–1.0000), where the term is inert — a missing
   term would diverge there too.
2. **Grazing angle is the amplifier:** far camera R=1.0052 G=1.0025 B=1.0056;
   the test's close 60° framing R=1.0257 G=1.0154 B=1.0743. Chromatic background
   contributes only ~0.3%.
3. **Decisive:** neutral `[0.35,0.35,0.35]` gives B=**1.0074**; chromatic with the
   *same* B=0.35 gives B=**1.0743**. Same channel value, 10× the divergence, the
   only variable being whether the *other* channels differ. Channel spread
   collapses 0.0589 → 0.0023 on a neutral albedo.

**Owner chose (a):** merge with a documented roughness-0.9-only band of
`[0.95, 1.10]` — not an xfail, which would make future regressions there invisible,
and not a global widening. 1.0722 keeps ~3% headroom, so a genuine regression still
fails. The spectral-vs-RGB mismatch is being filed as its own package, with
restoring `[0.95, 1.05]` at all roughnesses in its definition of done, so the
exception is self-retiring.

## In flight

- Nothing. All three code PRs merged; **pkg163** filed (spectral-vs-RGB compensation
  colour-space parity) and **pkg158** narrowed rather than closed.

### Two corrections from the pkg160 implementer, both accepted

- **Headroom on the r=0.9 exception is 2.6%, not ~3%** — my figure was loose; it used
  the accurate one and verified by probe rather than eyeball (1.09 PASS, 1.101 FAIL,
  0.94 FAIL).
- **The asymmetric band is load-bearing, not cosmetic.** The floor stays 0.95 at every
  roughness because the divergence is *one-directional* (GPU brighter). Widening the
  floor would have been the hole; widening only the ceiling keeps a GPU-*dim*
  regression failing.

### And a scoping refinement worth keeping

The architect established the *parity defect* is metal-only (metal is the only
material with a native per-λ CPU side facing an RGB GPU twin — Disney's CPU
`evalSpectral` is itself an RGB-upsample fallback, so its twins agree; glass
compensation is scalar). The implementer added that the *architectural seam* is not
metal-only: the same RGB→spectral boundary already carries the Fresnel term in both
metal paths, plus Disney's compensation and `ggxDirectionalAlbedo`. **The defect is
metal-only; the decision is not.** pkg163 is being re-framed accordingly.

Detection heuristic recorded: pkg160 made this measurable only because it put a
**roughness-amplified** factor through that seam. Small or view-independent factors
hide there indefinitely.

## pkg159 verification — and the limit of what I could prove

Gates green on RTX at `5b20790`: 4 passed, and the cross-path IoU the implementer
flagged reads **0.964–0.984 against a 0.85 threshold**.

That threshold was its one genuinely untested number — derived CPU-vs-CPU and
never exercised across paths. It holds with margin, and more importantly **it
discriminates**: the GPU leg's values are *distinct* from the CPU leg's
(0.9743 vs 0.9843). Had GPU crypto not been written at all, the buffers would be
all-zero and IoU ~0, not 0.97.

**What I could not prove:** a true cross-binary RED run. The gate file depends on
helpers this PR adds to `test_cryptomatte_pass.py`, so it cannot be run against
main's unported binary, and the standalone probe scene needs `bpy`. I made three
attempts and stopped — the distinct-IoU evidence is a stronger substitute, but it
is not the same claim and the PR records it as unverified rather than blurring
the two. Also unverified: addon-side pass packing of the GPU crypto buffers.

## Shipped today

| PR | What | Gate |
|---|---|---|
| **#528** | `fix(pkg162)`: last phantom `launchStageInit` overload corrected, shadow deleted — `05b6b49` | CI all-pass + independent different-model SIGN-OFF + measured behaviourally inert |
| **#529** | `feat(pkg159)`: GPU cryptomatte restored in the wavefront — `78e0ae4` | CI all-pass + RTX HW PASS bound to head `5b20790` |
| **#527** | `fix(pkg160)`: plain metal was **creating energy** — Kulla & Conty compensation replaces an invented additive term, plus the plain-`metal` parity gate that never existed — `2d5bb27` | CI all-pass + RTX HW PASS: 32/32 own gates, 384/0 metal-adjacent, full suite 4 failed / **1563** passed (same four known exceptions as main, +32) |

I authored #528 myself rather than dispatching it, so I put it through the same
independent different-model review I have held every other PR to — arguably more
necessary there, not less, since nothing else was checking my reasoning. The
reviewer supplied a **stronger** inertness argument than my measurement did:
default arguments are not part of a function's type or mangled signature, so
removing `= 0` cannot change linkage for any caller that already passes the
argument explicitly, and all six do. Overload resolution was unambiguous by arity
before and after; the same symbol links either way.
