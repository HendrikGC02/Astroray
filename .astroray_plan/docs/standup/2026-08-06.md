# Standup

## Shipped today
- #541 — CI-green + hardware-PASS

## In-flight
- (none)

## Blocked
- (none)

## Hardware gate
- GPU lock holder: (free)
- HW-untested queue: (empty)

## CI under repair
- (none)

## Action items
- Worktree GC: pkg165-metal-dim-diagnosis: no PR or PR query failed → skip
- Worktree GC: pkg171-cpu-only-integrator-gpu-guard: no PR or PR query failed → skip
- OWNER: queue is idle by design — with #541 merged (pkg168 closed), the remaining
  settlement work (pkg172 effect A + pkg174) is pinned to the SUPERVISED round and
  will not be auto-dispatched; the Integration Milestone (pkg175/pkg177) is sequenced
  behind settlement. Backlog fillers (pkg129/pkg165/pkg167) are also held back: their
  parity/energy numbers would be invalidated by pkg172(A)'s ~0.63%/bounce fix and
  the coordinated re-pin batch. Autonomous ticks stay idle until the owner runs the
  supervised round or explicitly releases pkg175/pkg177 early.
- pkg165 and pkg171 worktrees hold uncommitted work (probe scripts / module edits)
  with no running agent and no PR — stalled from a prior session; owner to resume or
  discard.
- `build_cuda/astroray.cp313-win_amd64.pyd` predates HEAD — rebuild before trusting
  any GPU result on main.

<!-- finalized -->
