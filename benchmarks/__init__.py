"""Astroray benchmark namespace package.

- `benchmarks.cycles-parity/` (pkg71) is *not* a Python package — its
  hyphenated name is intentional and it is not importable. Use the
  scripts under that directory directly.
- `benchmarks.showcase` (pkg74) is a Python package; see its README.
"""
