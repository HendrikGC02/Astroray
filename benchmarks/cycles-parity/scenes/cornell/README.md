# Cornell Box

Astroray-internal MIT control scene for pkg71. The standalone renderer already
ships this scene as `--scene 1`; the descriptor next to this README lets the
parity harness treat it like the downloaded Blender demo scenes without
committing binary data.
