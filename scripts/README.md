# Scripts

- `autonomous_loop.sh` — local autonomous engineering loop helper.
- `build_cuda.bat` — Windows helper script for CUDA-related builds.

These scripts are optional developer utilities and are not required to build or
test Astroray.
