# CUDA Smoke Sources

CUDA smoke sources are small harnesses used by optional CMake targets. They are
kept separate from runnable scripts because they are compiled test programs,
not Python or shell entrypoints.
